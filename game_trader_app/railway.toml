[build]
dockerfilePath = "Dockerfile"

[build.args]
# Set this to your Railway backend service URL
GAMEHUB_BASE_URL = "${GAMEHUB_BASE_URL}"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
